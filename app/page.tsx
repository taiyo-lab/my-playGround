// page.tsx
// use client
import { useState } from "react";

const page = () => {
  // const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count</p>
      <button>Increment</button>
    </div>
  );
};

export default page;
